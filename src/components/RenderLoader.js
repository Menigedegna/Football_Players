/**
* @description This component loads div component to be rendered while components are fetching data from server
* @returns {ReactNode} A React element which renders a div component
*/
const RenderLoader = () => <div className='loadingMessage'> ⛵ Loading ...</div>
export default RenderLoader;